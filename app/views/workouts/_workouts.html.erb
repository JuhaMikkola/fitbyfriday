
<% if current_user.workouts.any? %>

  <div class="workout-week">
    <% @workouts_by_day.each do |w| %>
      <div class="workout-date row">
        <div class="small-12 column workout-day days">
          <h3><time datetime="<%= w[0] %>"><%= w[0].strftime("%A, %b %d") %></time></h3>
        </div>

        <% w[1].each do |workout| %>
          <div class="workout row">
            <div class="small-12 column">
              <h3 data-time="<%= workout.start_time.utc.strftime("%H:%M:%S") %>"> <%= link_to workout.activity, workout_path(workout.id) %></h3>
              <h5 class="workout-details"><%= workout.start_time.utc.strftime("%l:%M%P") %> at <%= workout.location.capitalize %></h5>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <% else %>

<br>
<br>
<article>
<div class="quote">
    Our greatest weakness lies in giving up. The most certain way to succeed is always to try just one more time. <br>
   <small>- Thomas A. Edison</small>
</div>

 <article>

  <% end %>


  <h3>No Workouts Scheduled</h3>

<% end %>

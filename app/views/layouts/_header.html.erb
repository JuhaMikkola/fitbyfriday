    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <!-- Title Area -->
        <li class="name">
          <h1>
             <h1><%= link_to "Fit By Friday", root_path %></h1>
          </h1>
        </li>

        <li class="toggle-topbar menu-icon">
         <a href="#"><span></span></a>
        </li>
      </ul>


        <section class="top-bar-section">
        <!-- Right Nav Section -->
          <ul class="right">

            <li class="divider"></li>
            <li><%= link_to "Dashboard", workouts_path %></li>

            <% if current_user %>
              <li class="divider"></li>
              <li><%= link_to "Profile", user_path(current_user.id) %></li>
            <% end %>

            <li class="divider"></li>
            <li><%= link_to "Friends", users_path %><li>

            <% if current_user %>
            <li class="divider"></li>
            <li><%= link_to "Logout", :logout, method: :post %></li>
            <% else %>
            <li class="divider"></li>
              <li><%= link_to "Login", :login %></li>
            <% end %>

            </ul>

    </section>
  </nav>